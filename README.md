A simple, clean, and fully functional Android Notes application built using Room Database, Kotlin Coroutines, StateFlow, Search, Tags Filtering, Add Note Bottom Sheet, and two separate UI implementations:

XML UI (xml-impl branch)
Jetpack Compose UI (compose-impl branch)
Shared backend code (main branch)
